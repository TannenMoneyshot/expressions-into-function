// Clean up the code below with a single function called `personalIntroduction`
// The new code should have the exact same functionality and output.
const introduction = (name, height, favoriteColor) => {
    return `My name is ${name} and my height is ${height} and my favorite color is ${favoriteColor}`;
};

console.log(introduction("Bob", "72", "red"));
console.log(introduction("Samantha", "65", "orange"));
console.log(introduction("Christopher", "69", "green"));
console.log(introduction("Tamera", "63", "blue"));